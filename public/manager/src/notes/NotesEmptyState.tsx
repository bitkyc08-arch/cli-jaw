export function NotesEmptyState() {
    return (
        <section className="notes-empty-state">
            <strong>No notes yet</strong>
            <span>Create a markdown note from the file tree.</span>
        </section>
    );
}
