# Phase 4.3: Save 버튼 고정 + 탭 통합 + 프롬프트 모달

## 변경 사항

### 1. 탭 통합 (3탭 → 2탭)

```
Before: 🤖 Agents | ⚙ Config | 🔧 Settings
After:  🤖 Agents | 🔧 Settings
```

**Agents 탭** = 기존 Config + Sub Agents:
- Active CLI, Permissions, Working Directory
- `---` 구분선
- Sub Agents 리스트 + Add 버튼

### 2. Save 버튼 — 탭바 아래 고정

```
┌─── 🤖 Agents │ 🔧 Settings ──┐
│ [Save]                         │
├────────────────────────────────┤
│ (탭 내용)                      │
```

탭별 Save 동작:
- Agents 탭 → `updateSettings()` (CLI, permissions, CWD)
- Settings 탭 → `savePerCli()` (per-CLI model/effort)

### 3. 시스템 프롬프트 모달

Settings 탭 최상단 `✏️ 시스템 프롬프트 편집` 버튼:

```
┌── System Prompt (A-2) ──── ✕ ──┐
│  (textarea)                     │
│                  [취소]  [저장]  │
└─────────────────────────────────┘
```

- 열기: `/api/prompt` GET → textarea 채우기
- 저장: PUT `/api/prompt` → `regenerateB()` → modal 닫기
- 닫기: ESC / 백드롭 / ✕ / 취소

## 체크리스트

- [x] 3탭 → 2탭 통합 (Config → Agents에 합침)
- [x] Save 버튼 (`.sidebar-save-bar`)
- [x] `handleSave()` 탭별 분기
- [x] `✏️ 시스템 프롬프트 편집` 버튼
- [x] 모달 HTML/CSS (overlay + box)
- [x] `openPromptModal()` / `closePromptModal()` / `savePromptFromModal()`
- [x] ESC 키 닫기
- [x] inline textarea 제거, `loadPrompt()` 제거
